/* eslint-env browser */
/* eslint strict: off */

if(!Object.setPrototypeOf)
    Object.setPrototypeOf = function() { };

if(!window.Blob)
    window.Blob = function() { };

if(!window.FormData)
    window.FormData = function() { };
